## 1. 固化基线与可测试引擎接口

- [x] 1.1 保存并命名 `legacy-three-star` 的旧三星实现/配置，记录当前代码版本、权重、深度和随机行为，确保后续新引擎修改不影响基线
- [x] 1.2 从 UI 落子流程中提取“输入局面并返回分析结果”的引擎接口，结果包含落点、候选、战术分类、深度、节点数、预算状态和耗时
- [x] 1.3 为用户、测试和评测模式实现独立的每实例 PRNG 上下文，移除三星决策对共享全局 `srand/rand` 状态的依赖
- [x] 1.4 建立旧三星固定种子黄金局面测试，验证基线落点序列和重复运行一致性

## 2. 修复三星搜索正确性

- [x] 2.1 修复棋盘活动边界的 x/y 赋值、上下限裁剪和空棋盘初始化，并覆盖四边及四角局面
- [x] 2.2 修复未来攻击扫描、对方反击判断等已确认的坐标混用和错误目标点调用
- [x] 2.3 修复连续强攻递归中横线、竖线被整体排除的问题，统一四方向候选过滤条件
- [x] 2.4 移除强攻候选在首个命中行后的过早截断，完整收集、去重并稳定排序不可遗漏的战术候选
- [x] 2.5 统一 `chessid`、总手数、棋盘导入、make/unmake 和终局状态更新，保证分析前后状态完全一致
- [x] 2.6 增加立即胜、唯一防守、无解败势、禁手、边缘、旋转/镜像一致性和局面不污染回归测试

## 3. 参数化定向深搜

- [x] 3.1 定义可版本化 `AIProfile`，覆盖战术深度、普通候选 Top-K、节点/时间预算、置换表容量、评估权重和随机参数，并支持导出快照
- [x] 3.2 实现立即胜负、必要防守、强制进攻、强制防守和普通候选的分层生成与去重，确保战术层不受普通 Top-K 裁剪
- [x] 3.3 为冲四、四三、双三和连续强攻实现预算感知的迭代加深，预算耗尽时只采用最后完整轮次
- [x] 3.4 实现包含棋盘、行棋方、禁手模式、搜索类型、剩余深度和配置版本的置换表及命中统计
- [x] 3.5 将启发式评分改为确定性基础评分，恢复并校准必要的攻防对称项，删除直接叠加到候选分数的随机噪声

## 4. 实现安全随机决策

- [x] 4.1 为每个候选执行战术安全检查，排除对手立即取胜、非法着和已证明败势候选
- [x] 4.2 根据 Top-K 与最佳分差构造稳定排序的近优集合，并对必胜、唯一必要防守和单一安全候选走确定性路径
- [x] 4.3 实现按 `exp((score-best)/temperature)` 的 Softmax 带权抽样，加入随局面阶段、战术强度和分差收缩的温度策略
- [x] 4.4 增加随机策略测试：固定种子完全复现、实例间随机流隔离、唯一战术着跨种子不变、平稳近优局面跨种子出现多个安全落点

## 5. 建立自对弈与参数实验工具

- [x] 5.1 实现无需 UI 的自动对局运行器，支持两个引擎/AIProfile、独立种子、交换颜色、禁手规则、节点预算、最大手数和异常终止
- [x] 5.2 为每局保存开局标识、配置和种子、完整棋谱、结果、终止原因、逐步耗时、节点、深度、预算和缓存统计
- [x] 5.3 建立互不重叠的训练种子集与最终保留种子集，并验证所有生成开局合法、未提前结束且配对交换颜色一致
- [x] 5.4 实现参数 AB 汇总，输出总体及分颜色胜和负、得分率、Wilson 95% 置信区间和耗时/节点分位数
- [x] 5.5 验证相同配置、开局、规则、预算和主种子的重复比赛产生相同棋谱及汇总结果

## 6. AB 调参并接入生产三星

- [x] 6.1 在训练种子上对当前约 8/8/10 深度基线、10/10/12、12/12/14 及候选宽度组合进行首轮配对对抗，淘汰棋力或 p95 耗时明显较差的组
- [x] 6.2 对晋级组实验评估权重、近优分差、随机温度和最大随机候选数，验证多样性提升且战术回归零退化
- [x] 6.3 根据训练赛胜负、分先后手表现、性能和稳定性选择并冻结最终生产三星 AIProfile，记录选择依据
- [x] 6.4 将 iPhone 与 iPad 的三星入口接入同一个生产配置，确认一星、二星入口和现有存档行为未发生非预期变化
- [x] 6.5 在模拟器/真机执行构建、核心棋局测试和代表局面性能测试，确认用户思考时长与交互线程安全可接受

## 7. 运行最终 200 局验收赛

- [x] 7.1 冻结代码、旧三星基线、新三星配置、规则、预算、100 个保留开局和主种子，并生成赛前清单及复现命令
- [x] 7.2 运行新三星执黑对旧三星执白的 100 局对抗，保存全部原始逐局结果且核对局数与颜色
- [x] 7.3 使用同一批配对开局运行新三星执白对旧三星执黑的 100 局对抗，保存全部原始逐局结果且核对局数与颜色
- [x] 7.4 检查 200 局中的非法着、状态污染、崩溃、超时及其他异常；若正式赛无效，修复后从冻结赛程起点完整重跑，不得静默替换单局
- [x] 7.5 汇总总体、新三星执黑和新三星执白的胜和负、得分率、Wilson 95% 置信区间，以及双方耗时、节点、深度、预算和缓存统计

## 8. 生成并交付 Markdown 测试报告

- [x] 8.1 在仓库内生成带 UTC 时间和配置版本的 Markdown 报告及机器可读原始结果，包含规则、环境、代码/参数版本、种子方案、复现命令、分颜色结果、置信区间、性能、异常和代表棋局
- [x] 8.2 计算原始结果文件校验值并写入报告，复核报告中的总体与分组数字能从原始 200 局数据重新计算得到
- [x] 8.3 获得目标目录写权限后，将报告和原始结果交付到 `/Users/wangzicheng/Downloads/logs_five_chess`，校验复制前后文件一致且最终 `.md` 可读取
- [x] 8.4 记录最终报告绝对路径并确认新三星执黑 100 局、新三星执白 100 局及全部验收证据齐全
