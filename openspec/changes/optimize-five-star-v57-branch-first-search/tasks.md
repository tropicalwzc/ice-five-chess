## 1. Profile and telemetry contract

- [x] 1.1 Add opt-in branch-first profile fields, the v5.7.2 depth-14 profile factory, and snapshot fields while preserving existing profile defaults.
- [x] 1.2 Add branch preview/wave/job/useful-work/concurrency/depth/fallback diagnostics and expose them in the fixed-position runner/benchmark JSON.

## 2. Deterministic preview and recursive scheduler

- [x] 2.1 Add bounded advanced-four/advanced-three shallow preview scoring and canonical root threat ordering with deadline fallback.
- [x] 2.2 Carry branch-first configuration through the proof context and make the candidate route root obligations serial rather than root-parallel.
- [x] 2.3 Implement the long-recursion eligibility gate and private branch-wave task data using the existing persistent pool, including worker-side nested-dispatch suppression.
- [x] 2.4 Implement private child proof execution, shared token/ledger/deadline enforcement, deterministic result collection, and branch certificate parent-index remapping.
- [x] 2.5 Preserve serial fallback, proof/disproof aggregation, board restoration, and verified-certificate semantics for unknown, overflow, and deadline cases.

## 3. Tests and research tooling

- [x] 3.1 Add focused unit coverage for profile isolation, preview ordering, recursive branch diagnostics, certificate merge, and one/four/eight-worker determinism.
- [x] 3.2 Extend the fixed 97-position runner and replay/report scripts with a branch-first mode and depth/worker metadata.
- [x] 3.3 Run the AI unit suite, benchmark build, fixed-position baseline/scheduler/branch-first comparisons, and branch threshold/depth scans.
- [x] 3.4 Run the existing 200-game four-star and legacy-three-star replays with branch-first enabled and validate legality, certificates, deadlines, and deterministic replay.
- [x] 3.5 Publish the branch-first research report and record whether useful recursive work or strength improved enough to change the production route.
